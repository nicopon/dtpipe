
namespace QueryDump.Adapters.Parquet;

internal static class ParquetConstants
{
    public const string ProviderName = "parquet"; // This one is actually "parquet" not a short alias? User said "parquet" in summary.
    // User summary: "csv, parquet, sqlite, sample remain unchanged as they were already short".
}
